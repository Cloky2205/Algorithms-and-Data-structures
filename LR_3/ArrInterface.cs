public interface ArrInterface
{
    bool find(long searchValue);
    long findmax();
    long findmin();
    bool insert(long value);
    bool delete(long value);
    void display();
    int getSize();
}

